<section class="card">
  <header>
    <a class="comments" id="{{id}}">
    {{{name}}}
    </a>
    <span>
      Posted by {{{user.name}}} on {{{format_date date_created}}}
    </span>
  </header>
  <div class="card-body">
    <p>{{{description}}}</p>
  </div>
</section>